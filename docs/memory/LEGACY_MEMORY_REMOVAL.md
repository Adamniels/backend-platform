# Legacy memory — what to delete later

This list is the **full inventory** of the first‑draft “insights” feature kept alongside the new Memory module structure. When governed memory (and a replacement UI/API) is ready, follow the checklist in order so nothing is left half‑removed.

**Database table name:** `MemoryInsights` (PostgreSQL, created in `20260424063351_InitialCreate`).

**HTTP path:** `GET /api/v1/memory/insights`

---

## Backend (`backend-platform`)

### Domain

| Item | Path |
| --- | --- |
| Entity | `src/Platform.Domain/Features/Memory/Legacy/MemoryInsight.cs` |
| Namespace | `Platform.Domain.Features.Memory` (must match EF model snapshot until removed) |

### Contracts

| Item | Path |
| --- | --- |
| DTO | `src/Platform.Contracts/V1/MemoryInsightDto.cs` |

### Application

| Item | Path |
| --- | --- |
| Port | `src/Platform.Application/Abstractions/Memory/Legacy/ILegacyMemoryInsightsReadRepository.cs` |
| Query + handler | `src/Platform.Application/Features/Memory/Legacy/Insights/` (`ListMemoryInsightsQuery`, `ListMemoryInsightsQueryHandler`) |

**DI (remove registrations):** `src/Platform.Application/Features/Memory/DependencyInjection/MemoryApplicationServiceCollectionExtensions.cs` — `ListMemoryInsightsQueryHandler`

### Infrastructure

| Item | Path |
| --- | --- |
| Repository | `src/Platform.Infrastructure/Features/Memory/Legacy/LegacyMemoryInsightsReadRepository.cs` |

**DI (remove registration):** `src/Platform.Infrastructure/Features/Memory/DependencyInjection/MemoryInfrastructureServiceCollectionExtensions.cs` — `ILegacyMemoryInsightsReadRepository` → `LegacyMemoryInsightsReadRepository`

### API

| Item | Path |
| --- | --- |
| Routes | `src/Platform.Api/Features/Memory/Legacy/Insights/MemoryInsightsV1Routes.cs` |
| Composition | `src/Platform.Api/Features/Memory/MemoryV1Routes.cs` — remove `MemoryInsightsV1Routes.Map(v1)` and the `using` for `Legacy.Insights` |

### Persistence (EF / migrations)

| Item | Action |
| --- | --- |
| `PlatformDbContext` | Remove `DbSet<MemoryInsight>`, remove entire `modelBuilder.Entity<MemoryInsight>(...)` block (including `HasData` seed). File: `src/Platform.Infrastructure/Persistence/PlatformDbContext.cs` |
| New migration | Add a migration (e.g. `RemoveMemoryInsights`) that **drops** table `MemoryInsights` — do **not** edit `20260424063351_InitialCreate` |
| Model snapshot | Regenerated by the new migration: `src/Platform.Infrastructure/Persistence/Migrations/PlatformDbContextModelSnapshot.cs` |
| Old migration files | `20260424063351_InitialCreate.*` — **leave as historical**; the new migration supersedes the live schema |

### Tests

Grep the repo for `MemoryInsight`, `memory/insights`, `ListMemoryInsights` after removal and fix any stragglers.

---

## Frontend (`frontend-platform`)

| Area | Path / note |
| --- | --- |
| API adapter | `src/lib/api/adapters/insights.ts` — `fetchInsights`, type `MemoryInsight`, URL `/api/v1/memory/insights` |
| UI | `src/modules/insights/` (`InsightsView.tsx`, `InsightsScreen.tsx`, `api/get-insights.ts`, `index.ts`) |
| App route | `src/app/insights/page.tsx` |
| Nav / links | `src/components/layout/nav-items.ts`, `src/lib/shell/route-meta.ts`, `src/modules/dashboard/DashboardClient.tsx`, `src/modules/jarvis/JarvisScreen.tsx`, `src/modules/start/StartScreen.tsx`, `src/components/layout/AppShell.tsx` (push to `/insights`) |
| Smoke tests | `tests/smoke/adapters.test.ts` (mock path `"/api/v1/memory/insights"`), `tests/smoke/route-groups.test.tsx` (`MemoryInsight` type) |

**Later:** either remove the “Insights” product surface entirely or point it at the new memory API and types when that exists.

---

## Docs to update when legacy is gone

- [MEMORY_MODULE_README.md](MEMORY_MODULE_README.md) — remove references to legacy routes and `MemoryInsightDto`
- [IMPLEMENTATION_PLAN.md](IMPLEMENTATION_PLAN.md) — any rows still mentioning temporary legacy retention

---

## Removal checklist (suggested order)

1. **Ship** replacement data/API for the UI (if the Insights page stays).
2. **Backend:** delete Application / Infrastructure / Api / Contracts / Domain legacy files; strip `PlatformDbContext` and `MemoryV1Routes` wiring; remove DI lines.
3. **Add EF migration** to drop `MemoryInsights` and update snapshot; run against dev DB.
4. **Frontend:** remove or rework adapters, module, tests, and navigation as decided.
5. **Grep** the monorepo for: `MemoryInsight`, `MemoryInsights`, `memory/insights`, `Legacy/Insights`, `ILegacyMemory`, `MemoryInsightDto`, `ListMemoryInsights`, `fetchInsights`, `/insights`.
6. **Update** docs in this folder.

This file can be **deleted** once legacy removal is complete and the checklist has been run.
